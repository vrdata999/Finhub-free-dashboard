# Secrets — never commit real keys
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
venv/
env/

# Dashboard SQLite cache
dashboard/.cache/

# JSON dumps from `finnhub_free.py --json` (~9 MB, embed a key fragment)
finnhub_*.json

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json
dashboard/.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/